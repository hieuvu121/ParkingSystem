# Auth Page UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Sign In / Sign Up auth page with a tab switcher, pill-shaped inputs, and a golden Continue button — UI only, no API calls.

**Architecture:** Feature folder under `src/components/auth/` holds the four auth-specific components; shared `src/components/ui/` holds three primitives reusable across the app. `App.jsx` renders `<AuthPage />` directly (no router for now).

**Tech Stack:** React 18, Vite, Tailwind CSS 3 (already installed). No test framework — verification is visual via the Vite dev server.

---

## File Map

| Action | Path | Responsibility |
|---|---|---|
| Create | `src/components/ui/InputField.jsx` | Labelled pill input primitive |
| Create | `src/components/ui/PillButton.jsx` | Full-width golden pill button |
| Create | `src/components/ui/AvatarPlaceholder.jsx` | Centered gray circle |
| Create | `src/components/auth/AuthTabs.jsx` | Sign In / Sign Up tab switcher |
| Create | `src/components/auth/SignInForm.jsx` | Avatar + username + password + forgot |
| Create | `src/components/auth/SignUpForm.jsx` | 5-field registration form |
| Create | `src/components/auth/AuthPage.jsx` | Card shell + tab state |
| Modify | `src/App.jsx` | Render `<AuthPage />` |

---

### Task 1: ui/InputField — labelled pill input

**Files:**
- Create: `src/components/ui/InputField.jsx`

- [ ] **Step 1: Create the file**

```jsx
export default function InputField({ label, value, onChange, type = 'text', placeholder = '' }) {
  return (
    <div className="flex flex-col gap-2">
      <label className="text-lg font-bold text-black">{label}</label>
      <input
        type={type}
        value={value}
        onChange={onChange}
        placeholder={placeholder}
        className="w-full rounded-full border-2 border-[#3A3A3A] bg-transparent px-5 py-3 text-base outline-none"
      />
    </div>
  );
}
```

- [ ] **Step 2: Verify the file exists**

```bash
ls src/components/ui/InputField.jsx
```
Expected: file listed, no error.

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/InputField.jsx
git commit -m "feat: add InputField UI primitive"
```

---

### Task 2: ui/PillButton — golden action button

**Files:**
- Create: `src/components/ui/PillButton.jsx`

- [ ] **Step 1: Create the file**

```jsx
export default function PillButton({ children, onClick, className = '' }) {
  return (
    <button
      onClick={onClick}
      className={`w-full rounded-full bg-[#F5D26B] py-4 text-xl font-bold text-black transition hover:brightness-95 ${className}`}
    >
      {children}
    </button>
  );
}
```

- [ ] **Step 2: Verify the file exists**

```bash
ls src/components/ui/PillButton.jsx
```
Expected: file listed, no error.

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/PillButton.jsx
git commit -m "feat: add PillButton UI primitive"
```

---

### Task 3: ui/AvatarPlaceholder — gray circle

**Files:**
- Create: `src/components/ui/AvatarPlaceholder.jsx`

- [ ] **Step 1: Create the file**

```jsx
export default function AvatarPlaceholder() {
  return (
    <div className="flex justify-center">
      <div className="h-48 w-48 rounded-full bg-[#D9D9D9]" />
    </div>
  );
}
```

- [ ] **Step 2: Verify the file exists**

```bash
ls src/components/ui/AvatarPlaceholder.jsx
```
Expected: file listed, no error.

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/AvatarPlaceholder.jsx
git commit -m "feat: add AvatarPlaceholder UI primitive"
```

---

### Task 4: auth/AuthTabs — Sign In / Sign Up switcher

**Files:**
- Create: `src/components/auth/AuthTabs.jsx`

- [ ] **Step 1: Create the file**

```jsx
export default function AuthTabs({ activeTab, onTabChange }) {
  return (
    <div className="flex rounded-full bg-[#4A4A4A] p-1">
      <button
        onClick={() => onTabChange('signin')}
        className={`flex-1 rounded-full py-4 text-xl font-bold transition ${
          activeTab === 'signin'
            ? 'bg-[#F5D26B] text-black'
            : 'bg-transparent text-white'
        }`}
      >
        Sign In
      </button>
      <button
        onClick={() => onTabChange('signup')}
        className={`flex-1 rounded-full py-4 text-xl font-bold transition ${
          activeTab === 'signup'
            ? 'bg-[#F5D26B] text-black'
            : 'bg-transparent text-white'
        }`}
      >
        Sign Up
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Verify the file exists**

```bash
ls src/components/auth/AuthTabs.jsx
```
Expected: file listed, no error.

- [ ] **Step 3: Commit**

```bash
git add src/components/auth/AuthTabs.jsx
git commit -m "feat: add AuthTabs component"
```

---

### Task 5: auth/SignInForm — avatar + credentials + forgot

**Files:**
- Create: `src/components/auth/SignInForm.jsx`

- [ ] **Step 1: Create the file**

```jsx
import { useState } from 'react';
import AvatarPlaceholder from '../ui/AvatarPlaceholder';
import InputField from '../ui/InputField';
import PillButton from '../ui/PillButton';

export default function SignInForm() {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');

  function handleSubmit(e) {
    e.preventDefault();
    if (!username || !password) return;
    console.log('sign-in', { username });
  }

  return (
    <form onSubmit={handleSubmit} className="flex flex-col gap-6">
      <AvatarPlaceholder />
      <InputField
        label="Username"
        value={username}
        onChange={(e) => setUsername(e.target.value)}
      />
      <InputField
        label="Password"
        type="password"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
      />
      <PillButton onClick={handleSubmit}>Continue</PillButton>
      <hr className="border-[#3A3A3A]" />
      <div className="text-center">
        <a href="#" className="text-sm text-gray-400 underline">
          Forgot Password?
        </a>
      </div>
    </form>
  );
}
```

- [ ] **Step 2: Verify the file exists**

```bash
ls src/components/auth/SignInForm.jsx
```
Expected: file listed, no error.

- [ ] **Step 3: Commit**

```bash
git add src/components/auth/SignInForm.jsx
git commit -m "feat: add SignInForm component"
```

---

### Task 6: auth/SignUpForm — 5-field registration form

**Files:**
- Create: `src/components/auth/SignUpForm.jsx`

- [ ] **Step 1: Create the file**

```jsx
import { useState } from 'react';
import InputField from '../ui/InputField';
import PillButton from '../ui/PillButton';

export default function SignUpForm() {
  const [name, setName] = useState('');
  const [phone, setPhone] = useState('');
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [confirm, setConfirm] = useState('');

  function handleSubmit(e) {
    e.preventDefault();
    if (!name || !phone || !email || !password || !confirm) return;
    if (password !== confirm) return;
    console.log('sign-up', { name, phone, email });
  }

  return (
    <form onSubmit={handleSubmit} className="flex flex-col gap-6">
      <InputField
        label="Name"
        value={name}
        onChange={(e) => setName(e.target.value)}
      />
      <InputField
        label="Phone number"
        value={phone}
        onChange={(e) => setPhone(e.target.value)}
      />
      <InputField
        label="Email"
        type="email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
      />
      <InputField
        label="Password"
        type="password"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
      />
      <InputField
        label="Confirm password"
        type="password"
        value={confirm}
        onChange={(e) => setConfirm(e.target.value)}
      />
      <PillButton onClick={handleSubmit}>Continue</PillButton>
    </form>
  );
}
```

- [ ] **Step 2: Verify the file exists**

```bash
ls src/components/auth/SignUpForm.jsx
```
Expected: file listed, no error.

- [ ] **Step 3: Commit**

```bash
git add src/components/auth/SignUpForm.jsx
git commit -m "feat: add SignUpForm component"
```

---

### Task 7: auth/AuthPage — card shell + tab state

**Files:**
- Create: `src/components/auth/AuthPage.jsx`

- [ ] **Step 1: Create the file**

```jsx
import { useState } from 'react';
import AuthTabs from './AuthTabs';
import SignInForm from './SignInForm';
import SignUpForm from './SignUpForm';

export default function AuthPage() {
  const [activeTab, setActiveTab] = useState('signin');

  return (
    <div className="flex min-h-screen items-start justify-center bg-[#F8F7F0] pt-16">
      <div className="w-full max-w-md rounded-3xl bg-[#F8F7F0] p-10">
        <AuthTabs activeTab={activeTab} onTabChange={setActiveTab} />
        <div className="mt-10">
          {activeTab === 'signin' ? <SignInForm /> : <SignUpForm />}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verify the file exists**

```bash
ls src/components/auth/AuthPage.jsx
```
Expected: file listed, no error.

- [ ] **Step 3: Commit**

```bash
git add src/components/auth/AuthPage.jsx
git commit -m "feat: add AuthPage shell with tab state"
```

---

### Task 8: Wire up App.jsx + visual smoke test

**Files:**
- Modify: `src/App.jsx`

- [ ] **Step 1: Replace App.jsx content**

```jsx
import AuthPage from './components/auth/AuthPage';

export default function App() {
  return <AuthPage />;
}
```

- [ ] **Step 2: Start the dev server**

```bash
npm run dev
```
Expected: Vite starts, URL printed (usually `http://localhost:5173`).

- [ ] **Step 3: Visual checks — Sign In tab**

Open the URL. Verify:
- Cream background fills the page
- Dark pill tab switcher at top; "Sign In" tab is golden, "Sign Up" is dark
- Gray circle avatar centered below the tabs
- "Username" and "Password" labels (bold, black) above pill inputs
- Golden "Continue" button below the inputs
- Thin horizontal rule + "Forgot Password?" link at the bottom

- [ ] **Step 4: Visual checks — Sign Up tab**

Click "Sign Up" tab. Verify:
- "Sign Up" tab turns golden, "Sign In" turns dark
- Five labelled pill inputs: Name, Phone number, Email, Password, Confirm password
- Golden "Continue" button at the bottom
- No avatar circle

- [ ] **Step 5: Console check**

Fill in Sign In fields and click Continue. Open browser devtools console. Verify:
```
sign-in {username: "..."}
```
Switch to Sign Up, fill all fields with matching passwords, click Continue. Verify:
```
sign-up {name: "...", phone: "...", email: "..."}
```

- [ ] **Step 6: Commit**

```bash
git add src/App.jsx
git commit -m "feat: wire AuthPage into App"
```
